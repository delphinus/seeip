#!/bin/sh
DIR=$(cd $(dirname $0);pwd)
PID=$DIR/pids/app.pid
PSGI=$DIR/app.psgi
STARMAN=/home/delphinus/perl5/perlbrew/perls/perl-5.16.1/bin/starman

cd $DIR
$STARMAN -l 127.0.0.1:18526 --pid $PID $PSGI

# vim:se ft=sh:
